[pytest]
testpaths = tests
python_files = test_*.py *_test.py
pythonpath = . core
filterwarnings =
    ignore::DeprecationWarning
markers =
    live_auth: tests requiring real Supabase authentication service
addopts = -m "not live_auth"
