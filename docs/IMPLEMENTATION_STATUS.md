# JASUSS Implementation Status & Production Readiness Audit

> **Audit Date:** 2026-09-20  
> **Phase:** 00-audit  
> **Baseline Test Suite:** `pytest -q` (255 passed, 1 failed — `test_worker_tasks.py` free-tier cap expectation, 51 subtests passed), `npm run build --prefix web` (Exit code 0, 8/8 routes generated).

---

## 1. Production Hardening Audit Matrix

| Area | Requirement (from docs) | Status | Evidence (file:line or test) | Notes |
| :--- | :--- | :--- | :--- | :--- |
| **SSRF Protection (Scan URL)** | Reject loopback, private (RFC1918), link-local (`169.254.0.0/16`), cloud metadata (`169.254.169.254`, `metadata.google`), reserved hostnames, non-HTTP schemes. Resolve DNS A/AAAA records to prevent DNS rebinding. | **Partial** | [`api/main.py:210-249`](file:///home/devxgamer/ai-qa-agent/api/main.py#L210-L249), [`tests/test_auth_crawl.py:40-55`](file:///home/devxgamer/ai-qa-agent/tests/test_auth_crawl.py#L40-L55) | Hostname/IP string checks exist in `ScanRequest.validate_url`. However, pre-scan DNS resolution (A/AAAA checks for DNS rebinding), allowed port enforcement (`SSRF_ALLOWED_PORTS`), and hex/octal IP normalization are **Missing** (Planned: Phase 2). |
| **SSRF Protection (Browser / Crawler)** | Playwright layer intercepts sub-requests, redirects, and web workers via `context.route("**/*")`. Abort blocked IPs, limit redirects (<=5), disable downloads. | **Missing** | [`crawler/crawler.py`](file:///home/devxgamer/ai-qa-agent/crawler/crawler.py) | No `context.route` network guard exists in crawler or interactive tester. Redirects to internal network resources currently not intercepted at the browser level (Planned: Phase 2). |
| **JWT Validation** | Verify JWT signature locally via JWKS (cached with TTL, refresh on unknown kid). Enforce `iss`, `aud`, `exp`, `nbf`, and algorithm allow-list. Reject `alg=none`. | **Partial** | [`api/main.py:261-294`](file:///home/devxgamer/ai-qa-agent/api/main.py#L261-L294) | `require_user` delegates verification remotely via `supabase.auth.get_user(token)`. Local PyJWT signature verification against Supabase JWKS endpoint with cached keys and claim enforcement is **Missing** (Planned: Phase 2). |
| **Tenant Ownership & Scoping** | Every scan, report, and artifact query must be strictly scoped by caller `user_id`. Cross-tenant requests must return HTTP 404. Admin role must come strictly from token claim, DB `users.role`, or exact `ADMIN_EMAILS` allowlist (no wildcards/patterns). | **Partial** | [`api/main.py:654`](file:///home/devxgamer/ai-qa-agent/api/main.py#L654), [`api/admin.py:20-60`](file:///home/devxgamer/ai-qa-agent/api/admin.py#L20-L60), [`tests/test_admin_api.py`](file:///home/devxgamer/ai-qa-agent/tests/test_admin_api.py) | Insecure `admin@*` email pattern removed in hotfix. Admin requires explicit token claim, database role, or `ADMIN_EMAILS` exact address. Centralized query scoping helper remains Planned for Phase 2. |
| **Rate Limiting** | Tiered rate limits per-user, per-IP, and dedicated limit on `POST /scans`. Redis sliding window token bucket with headers (`Retry-After`, `X-RateLimit-*`). | **Partial** | [`api/rate_limiter.py:35-85`](file:///home/devxgamer/ai-qa-agent/api/rate_limiter.py#L35-L85), [`tests/test_rate_limiter.py`](file:///home/devxgamer/ai-qa-agent/tests/test_rate_limiter.py) | Redis sliding window with memory fallback exists. However, standard response headers (`Retry-After`, `X-RateLimit-Limit`, `X-RateLimit-Remaining`) and dedicated `POST /scans` rate limits are **Missing** (Planned: Phase 2). |
| **Payment Webhooks** | Load per-gateway secrets from config; fail-closed 503 if unconfigured; constant-time HMAC on raw body (400 on bad sig); idempotent storage in `webhook_events` table. | **Partial** | [`api/billing.py:120-188`](file:///home/devxgamer/ai-qa-agent/api/billing.py#L120-L188), [`billing/gateways.py:45-310`](file:///home/devxgamer/ai-qa-agent/billing/gateways.py#L45-L310), [`tests/test_billing_webhooks.py`](file:///home/devxgamer/ai-qa-agent/tests/test_billing_webhooks.py) | Hardcoded mock secret removed. Per-gateway secrets loaded from config with fail-closed 503 and constant-time HMAC verification on raw body (400). `webhook_events` table and transactional idempotency remain Planned for Phase 2. |
| **Credential Handling & Redaction** | Write-only `SecretStr` for login credentials. Automatic redaction in logs, reports, AI prompts, and HAR. Strip Authorization/Cookies from HAR. | **Partial** | [`api/main.py:165`](file:///home/devxgamer/ai-qa-agent/api/main.py#L165), [`security/redactor.py`](file:///home/devxgamer/ai-qa-agent/security/redactor.py), [`core/gemini_analyzer.py:163-208`](file:///home/devxgamer/ai-qa-agent/core/gemini_analyzer.py#L163-L208), [`tests/test_redaction.py`](file:///home/devxgamer/ai-qa-agent/tests/test_redaction.py) | `SecretStr` is implemented on `ScanAuthPayload`. `SecretRedactor` actively sanitizes reports, logs, and Gemini prompts. Credentials are never saved in DB. However, HAR network capturing with cookie/header sanitization is **Missing** (Planned: Phase 2). |
| **Export Formula Injection Escaping** | Escape user-controlled and target-derived strings starting with `=`, `+`, `-`, `@`, tab, CR in Excel/CSV exports (prefix with `'`). | **Missing** | [`api/main.py:772-800`](file:///home/devxgamer/ai-qa-agent/api/main.py#L772-L800) | `_generate_xlsx_from_json` inserts defect titles and URLs directly into openpyxl cells without prefixing dangerous formula trigger characters (Planned: Phase 2). |
| **Celery Worker Configuration** | `acks_late=True`, `task_time_limit`, `worker_prefetch_multiplier=1`, `reject_on_worker_lost`, worker task recycling, separate priority queues per tier. | **Partial** | [`worker/celery_app.py:29-46`](file:///home/devxgamer/ai-qa-agent/worker/celery_app.py#L29-L46) | `task_acks_late=True`, `task_time_limit=1800`, `task_soft_time_limit=1700`, `worker_prefetch_multiplier=1` are configured. However, priority queues (`qa_default`, `qa_priority`, `qa_enterprise`), `reject_on_worker_lost`, and worker recycling (`worker_max_tasks_per_child`) are **Missing** (Planned: Phase 3). |
| **Scan Cancellation & Reaper** | Cancellation endpoint revokes Celery task and sets Redis flag; workers periodically check flag and clean up browser. Celery beat reaper marks stale scans failed. | **Partial** | [`api/main.py:713-731`](file:///home/devxgamer/ai-qa-agent/api/main.py#L713-L731) | `POST /scans/{id}/cancel` sets status to cancelled and calls `celery_app.control.revoke`. However, workers do not poll cancellation keys between pages, and Celery beat stuck-scan reaper is **Missing** (Planned: Phase 3). |
| **Storage Backend Abstraction** | Unified `StorageBackend` (LocalStorage, S3Storage / Cloudflare R2 / MinIO). Presigned download URLs. | **Missing** | [`api/main.py:824-870`](file:///home/devxgamer/ai-qa-agent/api/main.py#L824-L870) | All artifacts and reports are currently read and written directly to local filesystem paths (`user_data/`, `results/`). No S3 or boto3 integration exists (Planned: Phase 3). |
| **Database Schema & Migrations** | PostgreSQL enforced in production. Alembic migrations up to date with full schema (`findings`, `scan_artifacts`, extended scan metadata, composite indexes). | **Partial** | [`db.py:11-25`](file:///home/devxgamer/ai-qa-agent/db.py#L11-L25), [`alembic/versions/`](file:///home/devxgamer/ai-qa-agent/alembic/versions/) | Alembic revisions exist up to `003_add_subscriptions_and_plans.py`. Connection pooling (`pool_pre_ping=True`) is active. However, SQLite is not blocked in production, and tables `findings`, `scan_artifacts`, `webhook_events` from `docs/DATABASE.md` are **Missing** (Planned: Phase 3). |
| **Observability & Telemetry** | Structured JSON logging with `request_id` and `scan_id`, Prometheus `/metrics`, Sentry integration, `/healthz` and `/readyz` probes. | **Partial** | [`api/main.py:889-930`](file:///home/devxgamer/ai-qa-agent/api/main.py#L889-L930), [`tests/test_healthz.py`](file:///home/devxgamer/ai-qa-agent/tests/test_healthz.py) | `/healthz` (liveness) and `/readyz` (readiness) are implemented and tested. Structured JSON logging, `X-Request-ID` propagation middleware, Prometheus `/metrics`, and Sentry error tracking are **Missing** (Planned: Phase 4). |
| **Containerization & CI** | Split multi-stage Dockerfiles (`api.Dockerfile`, `worker.Dockerfile`, `web.Dockerfile`) with non-root user. CI pipeline with Postgres/Redis services, linting, security audits. | **Partial** | [`Dockerfile`](file:///home/devxgamer/ai-qa-agent/Dockerfile), [`docker-compose.yml`](file:///home/devxgamer/ai-qa-agent/docker-compose.yml), [`.github/workflows/ai-qa.yml`](file:///home/devxgamer/ai-qa-agent/.github/workflows/ai-qa.yml) | Root Dockerfile and `docker-compose.yml` exist. However, Dockerfile runs as `root`, split images do not exist, and CI workflow does not spin up DB services or run security tools (`ruff`, `mypy`, `bandit`, `gitleaks`) (Planned: Phase 5). |
| **Startup Config Validation** | Fail fast on boot in production if `DATABASE_URL` is SQLite, required secrets are missing, or CORS origins contain `*`. | **Partial** | [`config.py:13-47`](file:///home/devxgamer/ai-qa-agent/config.py#L13-L47) | Pydantic Settings class exists, but all fields default to None or fallbacks. Strict production validation is **Missing** (Planned: Phase 3). |

---

## 2. Identified Discrepancies Between Docs & Codebase

| # | Item | Docs Claim (with Citation) | Code Reality | Proposed Resolution |
| :--- | :--- | :--- | :--- | :--- |
| 1 | **Database Schema (Tables & Columns)** | [`docs/DATABASE.md:92-101`](file:///home/devxgamer/ai-qa-agent/docs/DATABASE.md#L92-L101) lists `progress_pct`, `started_at`, `error_code`, `celery_task_id`, `quality_score`, `grade`; [`docs/DATABASE.md:113-171`](file:///home/devxgamer/ai-qa-agent/docs/DATABASE.md#L113-L171) defines `findings`, `scan_artifacts`, and `webhook_events` tables. | Current Alembic migration head is `003_add_subscriptions_and_plans.py`. Findings are stored as JSON in report files. Artifacts are on the local filesystem. `webhook_events` table does not exist. | **Target State:** Note in `docs/DATABASE.md` that these tables represent the Target Schema. Implement Alembic migrations in Phase 3. |
| 2 | **Payment Webhook Idempotency** | [`docs/BILLING.md:67-94`](file:///home/devxgamer/ai-qa-agent/docs/BILLING.md#L67-L94) describes idempotent webhook processing via `INSERT webhook_events ... ON CONFLICT DO NOTHING`. | While gateway webhook secret verification and constant-time HMAC checking are now active in [`api/billing.py:120-188`](file:///home/devxgamer/ai-qa-agent/api/billing.py#L120-L188), the `webhook_events` table and transactional idempotency storage do not yet exist. | **Target State:** Document in `docs/BILLING.md` that `webhook_events` table and idempotency storage are scheduled for Phase 2 hardening. |
| 3 | **Progress Streaming via SSE** | [`docs/API.md:164-167`](file:///home/devxgamer/ai-qa-agent/docs/API.md#L164-L167) references `GET /scans/{id}/events` (Server-Sent Events). | Endpoint does not exist in `api/main.py`. The frontend polls `GET /api/scans/{id}` every 2 seconds. | **Fix Doc:** Clarify in `docs/API.md` that polling `GET /api/scans/{id}` is the current active implementation, and SSE `/scans/{id}/events` is planned. |
| 4 | **Prometheus Metrics** | [`docs/API.md:124`](file:///home/devxgamer/ai-qa-agent/docs/API.md#L124) and [`docs/OPERATIONS.md:20,43-57`](file:///home/devxgamer/ai-qa-agent/docs/OPERATIONS.md#L20) list `GET /metrics` and Prometheus scraping. | No `/metrics` route exists in `api/main.py` and `prometheus_client` is not installed. | **Target State:** Mark `GET /metrics` as Target / Planned in `docs/API.md`. Implement in Phase 4. |
| 5 | **Object Storage (S3 / R2)** | [`docs/ARCHITECTURE.md:70,184,231,328`](file:///home/devxgamer/ai-qa-agent/docs/ARCHITECTURE.md#L70) shows S3 Object Storage for PDFs, XLSX, HAR, and screenshots. | All files are saved locally to `user_data/<user_id>/` or `results/`. No `boto3` or S3 client exists. | **Target State:** Clarify in `docs/ARCHITECTURE.md` that local disk storage is current, with S3Storage abstraction scheduled for Phase 3. |
| 6 | **Render Blueprint Configuration** | [`docs/DEPLOYMENT.md:78,438`](file:///home/devxgamer/ai-qa-agent/docs/DEPLOYMENT.md#L78) references shipping both `render.yaml` and `render.yml`. | Only `render.yaml` exists in the repository; `render.yml` is absent. `render.yaml` healthCheckPath is set to `/healthz`. | **Fix Doc:** Confirmed and updated `docs/DEPLOYMENT.md` to reference `render.yaml` exclusively. |
